# ไม่เอาไฟล์ระบบของ Python ขึ้น
__pycache__/
*.py[cod]
*$py.class

# ไม่เอาไฟล์ความลับขึ้น (ถ้ามี)
.env
.streamlit/secrets.toml

# ถ้าไม่อยากให้เอกสารทดสอบที่ HR อัปโหลดหลุดขึ้น GitHub ให้เปิดคอมเมนต์บรรทัดล่างนี้
# hr_templates/
